const getSubArray = (arr, numberOfElements) => {
   let newArr = [];
   for (let i = o; i <= numberOfElements.length; i++) {
      
   }
}
let numbers = getSubArray([11, 4, 8, 3], 2);
console.log(numbers);